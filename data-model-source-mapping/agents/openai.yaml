interface:
  display_name: "数据模型源映射"
  short_description: "将数据源、指标、接口和前端字段映射为可追溯数据模型"
  default_prompt: "Use $data-model-source-mapping to create reusable data model files with source metadata, fields, relationships, metrics, transformations, permissions, and traceability."
