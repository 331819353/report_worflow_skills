# Runtime QA Note Template

```markdown
## Frontend Runtime QA Note

- Commands run:
- URL verified:
- Console status:
- Runtime provider status:
- Route/surface cases checked:
- Headless screenshots:
- Component crop screenshots:
- Component crop checklist:
- Proof obligations:
- Stacked chart continuity:
- ECharts SVG text overlap:
- KPI value-anchor DOM checks:
- Control ownership checks:
- Perspective navigation DOM checks:
- Fixed-height card budget/DOM checks:
- DOM structural checks:
- Duplicate title/label checks:
- Deterministic visual regression:
- Baseline diff artifacts:
- Multimodal visual review:
- Anti-AI runtime review:
- Report decision anti-AI review:
- Visual findings:
- Interaction cases checked:
- Perspective/control semantics checked:
- Cross-perspective consistency checked:
- Data states checked:
- Copy cleanup:
- Layout/clipping status:
- Evidence:
- Remaining blocker:
```

Use `Runtime provider status` for network APIs, GraphQL, SDK calls, static files, local fixtures, or realtime feeds.
Use `Deterministic visual regression` for baseline status: pass, fail, baseline missing, or not run, with threshold and masked-region notes.
Use `Baseline diff artifacts` for baseline/current/diff image paths from Playwright/Cypress or the project's visual regression setup.
Use `Visual findings` for `VDIFF-*` items from deterministic image diff and `VIS-*` items from multimodal screenshot review, including severity, screenshot path, diff path when available, component/region, likely owner, fix direction, and retest criteria.
Use `Anti-AI runtime review` for `AI-*` findings from `$report-design-system-governance` `references/08-anti-ai-design-gate.md`: thin product context, generic aesthetics, first-screen-only completion, generic copy, brandless/generic assets, average hierarchy, component soup, decorative motion, and sample-like engineering. Include `antiAiRisk`, evidence, fix direction, and approved exceptions.
Use `Report decision anti-AI review` for `RPT-*` findings from `$report-design-system-governance` `references/09-report-decision-anti-ai-gate.md`: metric shell, template dashboard layout, decorative chart choice, missing data story, too-clean data, static filters, visual-over-data, missing industry sense, missing action, and report-designer shell. Include `reportDecisionRisk`, evidence, fix direction, and readiness impact.
Use `Component crop screenshots` for donut/pie chart, Gauge chart, funnel chart, radar chart, scatter/bubble chart, parallel-coordinate chart, map/geographic chart, candlestick/K-line chart, boxplot chart, heatmap chart, sunburst chart, treemap/rectangular tree map, path/user/process path chart, Sankey diagram, tree/hierarchical tree chart, relation/network graph chart, Combo chart, Composite Panel, Analysis & Insight component, trend/cartesian chart, KPI card/group, Detail Table, Pivot Table, table with complex/grouped headers, and table/analytical-grid crops when present. Donut/pie, Gauge, funnel, radar, scatter/bubble, parallel-coordinate, map/geographic, candlestick/K-line, boxplot, heatmap, sunburst, treemap/rectangular tree map, path/user/process path, Sankey, tree/hierarchical tree, relation/network graph, Combo chart, Composite Panel, Analysis & Insight, Detail Table, Pivot Table, and complex/grouped-header table components require their own crops; do not accept only full-page evidence.
Use `Component crop checklist` for verifiable checks: Analysis & Insight analysisInsightContract/subtype/family, conclusion-before-evidence, evidence/affected object/comparison/change/reason/source/freshness or insufficient-data state, action/detail/trust/definition disclosure, local-filter scope, tooltip/detail payload, loading/empty/error/no-permission/data-delay states; Composite Panel compositePanelContract, one shared topic, analysis sequence, primary child id, child roles/priorities/min sizes, default 2-3 children and normal max 4, primary visual weight 50-70%, contentH >= CH * 0.60, panel-level local filter, child-only filter exception, shared legend/unit, linked hover/click state, short detail preview, responsive fallback, parent/child loading/empty/error/no-permission scope; Detail Table row grain/primary key/default sort, prioritized visible columns, tableBodyAreaH >= CH * 0.55, visible rows >=4-6 by default, numeric/status/action alignment, fixed header/frozen columns, pagination/search/export scope, row detail/action, tooltip disclosure; complex/grouped table header columnTree/nested columns, real leaf fields, colSpan/rowSpan or maxDepth/leafColumnCount, group-to-leaf alignment, fixed whole header, frozen row/primary columns, component-local filter vs column-filter separation, tooltip definitions, density fallback, useful body-row budget; Pivot Table visualType `pivot`, S2/project renderer, row/column dimensions, measures, aggregation formulas/functions, subtotal/grand-total, rate numerator/denominator rules, frozen headers, pivotAreaH, density fallback, exact cell tooltip/drilldown; Combo shared x-axis/bar-line-target metric roles, axis units, series <=4, dual-axis rationale, plotH >= CH * 0.48, exact tooltip, ECharts bottom legend/x-axis spacing, Gauge one bounded metric/min/max/unit/current value, `series.type: 'gauge'`, center value hierarchy, start/end angle, radius, center, arc width, target/threshold/status semantics, business color direction, clamp/overflow behavior, sparse ticks, local-filter fit, exact tooltip, scatter/bubble X/Y units, axis range/baseline, point density, label clutter, bubble mapping/overlap, reference/quadrant labels, parallel-coordinate object/sample schema, `3-12` ordered dimensions, axis unit/range/direction, standardization or independent-axis mode, `axisGap >= 56px`, `plotH >= CH * 0.48`, line opacity/sampling/aggregation, Top/anomaly/selected highlight, brush/filter/legend separation, exact object tooltip, map geography binding, map resource/projection, aspect-safe fitBounds, visualMap/legend placement, basemap/data-layer hierarchy, key-only labels, dense point clustering/heatmap, flow TopN readability, missing-geo states, candlestick OHLC fields/validity, market color convention, candle density/body width, main/volume height budget, volume alignment, MA/indicator overload, dataZoom/recent-window, crosshair/tooltip exact values, boxplot sample/statistical contract, sample count, Q1/median/Q3/IQR, whisker/outlier rule, median/IQR readability, category/group density, outlier overplot, five-number tooltip, heatmap row/column/value contract, visualMap/color scale, missing-vs-zero encoding, cell size, row/column label density, value-label threshold, all-cell text overload, exact-value tooltip, sunburst hierarchy/children or parent-path schema, non-negative additive angle metric, parent/child aggregation, visible depth/ring width, TopN/其他 aggregation, sector label thresholds, center content fit, color semantics/visualMap, breadcrumb/drilldown, exact path/value/total-share/parent-share tooltip, treemap hierarchy/children or parent-path schema, non-negative additive area metric, parent/leaf aggregation, TopN/其他 aggregation, visible depth, label thresholds, color semantics/visualMap, breadcrumb/drilldown, exact path/value/total-share/parent-share tooltip, funnel stage schema/order, shared population/cohort, entry share, stage conversion, drop value/rate, total conversion, stage-density fallback, target/comparison markers, exact tooltip, path chart step/link schema, start/end/order, metric basis, conversion/drop-off, TopN/其他 aggregation, branch density, key-only labels, arrow direction, legend/filter separation, node/link tooltip, Sankey source-target-value schema, stage/layer order, non-negative value handling, TopN/其他 aggregation, node/link density, flow-width mapping, main-flow emphasis, loss/unknown node handling, legend/filter separation, node/link tooltip, tree chart root node, parent-child schema, depth/layer, visible-depth/default-expanded rule, TopN/+N aggregation, expand/collapse, search/locate when dense, node label fit, connector clarity, legend/filter separation, node tooltip, relation graph node/edge schema, source/target direction, layout type, density strategy, hairball risk, label clutter, edge arrows, legend/filter separation, fitView, zoom/search/detail controls, node/edge tooltip, donut/pie category count, `TopN + 其他` merge rule, negative/all-zero handling, legend/label-line/label/title/center fit, `legendBandHeight`, `labelLineBudget`, `radius`, `innerRadius` for donut, `center`, right-legend width budget when used, low-value label handling, KPI value-zone centering and >=40% height, and any documented exception.
Use `Proof obligations` for pass/fail evidence that rules reached implementation: contract-to-DOM/CSS/option binding, KPI X/Y centering and CSS cascade, template/component control ownership, fixed-height overflow assertions, top-centered legend/grid option proof, source anchors, screenshot/crop paths, and non-default state checks. If a declared contract has no implementation proof, record `LAZY-CONTRACT-THEATER`.
Use `Stacked chart continuity` for ECharts stacked bar/column/progress charts: shared `stack` and category index, aligned source rows/series arrays, square internal joins, rounded corners only on the outer visible stack ends, `itemStyle.borderWidth: 0` unless an intentional divider is documented, no background sliver, overlap notch, or rounded internal seam at normal and narrow widths, and no full-pill radius applied to every stacked series.
Use `ECharts SVG text overlap` for inspectable SVG-rendered charts: measured legend text/marker, y-axis name, axis label, title, and annotation rectangles; intersection area threshold; declared safe gap; ignored parent-child/approved overlay pairs; and for dual-axis Combo with top legend, proof that legend items and left/right y-axis names keep at least `8px` gap with no overlap greater than `4px²`. For top-centered Combo legends, include `legend.left: 'center'` or equivalent centering plus `grid.top` evidence; right-anchored legends do not pass top-centered requirements.
Use `KPI value-anchor DOM checks` for metric cards: selectors or semantic roles used for card body, `.metric-value-line`, `.metric-value`, `.metric-unit`, `.metric-comparisons`, and `.metric-footer` or equivalents; measured `valueGroupRect`, `valueAnchorViewport`, `centerDeltaX`, `centerDeltaY`, value slot height ratio, numeral glyph height ratio, font-size/line-height, baseline-alignment check, computed CSS/cascade source for alignment, and any `VIS-KPI-VALUE-OFFCENTER` finding.
Use `Control ownership checks` for template-based pages: control ownership matrix for refresh, download/export, copy/share, global filters, period/date controls, topbar actions, local filters, and component micro actions; duplicate visible controls; disabled/hidden template counterparts; and any `VIS-DUPLICATE-CONTROL` or `RPT-SHELL-DUPLICATE` finding.
Use `Duplicate title/label checks` for per-block visible text ownership: block/card title, component body title, KPI body metric label, chart title, table caption, `displayTitle`, `metricName`, `showBodyMetricLabel`, normalization rule, comparison result, justified exceptions, and any `VIS-DUPLICATE-TITLE` finding.
Use `Perspective/control semantics checked` for non-default domain/theme/management-object/subject-area cases, including metric names, title/summary wording, table dimensions/headers, component set, specialty metrics, risk focus, and口径 labels.
Use `Perspective navigation DOM checks` for `1920x1080` and `1280x768` checks where visible domain nav/Tab/Segment item content satisfies `scrollHeight <= clientHeight + 2` and `scrollWidth <= clientWidth + 2`.
Use `Fixed-height card budget/DOM checks` for fixed-height navigation/cards/KPI tiles/compact summaries/summary cards/ranking cards/composite blocks: declared padding, explicit line-height, gaps, badge/status/footer heights, `requiredContentHeight <= componentHeight`, overflow CSS, and DOM overflow results. `scrollHeight > clientHeight + 2` or `scrollWidth > clientWidth + 2` is clipping unless it is an intentional visible scroll, expand/collapse, pagination, drawer/fullscreen, or split region.
Use `DOM structural checks` for expected vs rendered card/component counts, visible sibling bounding-box overlap checks, chart/table category uniqueness checks, selector/config source used, ignored nested/hidden/approved-overlay pairs, and resulting `VIS-*` or data-binding findings.
Use `Cross-perspective consistency checked` for field-level assertions tying navigation percentages to the same data chain as overview KPIs, journey cards, and chart summaries, such as navigation satisfaction equals current `experienceProfiles.satisfaction`.
