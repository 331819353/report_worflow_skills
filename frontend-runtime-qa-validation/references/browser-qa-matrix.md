# Browser QA Matrix

## Build And Startup

- Typecheck, lint, test, build, dev, preview, or static serve commands available in the project.
- Record skipped commands only when undefined or externally blocked.
- Confirm the served URL and route entry.

## Browser Basics

- Page loads without blocking runtime errors.
- Console has no blocking errors, repeated warnings, hydration failures, or asset load failures.
- Network/provider traces do not show full-materialize-then-filter behavior for global/page scope: global filter/search/page/sort UI changes should update request/query/resolver params rather than fetching all candidate rows and narrowing locally. Component-internal filters may stay local when they operate only on already fetched component data.
- Before filter UI interaction is judged, confirm data completeness evidence exists: filter option data, default rows, at least one non-default filter dataset/response, required fields, and empty/no-permission state when relevant. If a data source has only one default snapshot, filter linkage cannot pass for affecting filters.
- Static assets, chunks, fonts, images, workers, and downloaded templates resolve under the intended base path.
- Layout appears at target viewport sizes without obvious clipping or overlap.
- Domain navigation, Tabs, Segments, and first-level perspective controls are checked at `1920x1080` and `1280x768` with DOM overflow assertions. For each visible item/card content viewport: `scrollHeight <= clientHeight` and `scrollWidth <= clientWidth`. Screenshots can support findings but cannot replace these DOM checks.
- Fixed-height navigation/cards/KPI tiles/compact summaries are checked with DOM overflow assertions at `1920x1080` and `1280x768`. `scrollHeight > clientHeight` or `scrollWidth > clientWidth` is a clipping finding unless the element is an intentional visible scroll region.
- Repeated cards, KPI tiles, nav cards, journey cards, list rows, and component blocks are checked with DOM count assertions against the design/binding/config budget. A mismatch between expected count, rendered visible count, and overflow/pagination count is a `VIS-*` or data-binding finding.
- Within each visible report block/card, compare the block/header title with component-body titles, KPI body metric labels, chart option titles, table captions, and subcomponent labels. Normalize by trimming, removing whitespace and common separators, and standardizing punctuation. If `normalized(blockTitle) === normalized(bodyTitleOrMetricLabel)` or the texts are highly similar without a disambiguating qualifier, record `VIS-DUPLICATE-TITLE`.
- Visible peer cards, KPI tiles, chart containers, legends, filters, table headers, diagram nodes, and action controls are checked with bounding-box overlap assertions. Ignore pairs that are intentionally nested parent/child or hidden; flag visible sibling boxes whose intersection area is non-trivial and not an approved overlay.
- KPI/metric cards are checked with value-anchor bounding boxes when selectors or semantic roles are inspectable. Measure the card body, declared value slot or `.metric-value-line`, `.metric-value`, `.metric-unit`, `.metric-comparisons`, and `.metric-footer` or project-equivalent nodes. For default centered cards, `abs(valueGroup.centerY - valueAnchorViewport.centerY) <= 8px`; otherwise record `VIS-KPI-VALUE-OFFCENTER`. Verify the primary numeral glyph is not weak for the card size and that comparisons/footer do not overlap or push the value group upward.
- When ECharts uses the SVG renderer or exposes SVG text nodes, measure visible legend text/markers, y-axis names, axis labels, title text, and chart annotations with `getBoundingClientRect()` or SVG `getBBox()` converted to viewport coordinates. Text/marker pairs that are not parent-child labels and intersect by more than `4px²`, or have less than the declared safe gap, are `VIS-*` findings even when the outer chart container boxes do not overlap.
- Chart category axes, categorical legends, table grouped headers, and categorical datasets are checked for uniqueness at the rendered/config level. Duplicate category labels/ids fail unless the component declares an intentional grouping/stacking/aggregation rule and tooltip/detail can distinguish the duplicates.

## Headless Screenshot, Baseline Diff, And Multimodal Review

- Use a headless browser or browser automation tool to capture screenshots before declaring layout status.
- Capture first viewport and full page for the primary route.
- For report/dashboard pages, capture component-level crops in addition to full-page screenshots. Required crops when present: donut/pie chart, Gauge chart, funnel chart, radar chart, scatter/bubble chart, parallel-coordinate chart, map/geographic chart, candlestick/K-line chart, boxplot chart, heatmap chart, sunburst chart, treemap/rectangular tree map, path/user/process path chart, Sankey diagram, tree/hierarchical tree chart, relation/network graph chart, Combo chart, Composite Panel, Analysis & Insight component, trend/cartesian chart, KPI card or KPI group, Detail Table, Pivot Table, table with complex/grouped headers, and table/analytical grid. Donut/pie, Gauge, funnel, radar, scatter/bubble, parallel-coordinate, map/geographic, candlestick/K-line, boxplot, heatmap, sunburst, treemap/rectangular tree map, path/user/process path, Sankey, tree/hierarchical tree, relation/network graph, Combo chart, Composite Panel, Analysis & Insight, Detail Table, Pivot Table, and complex/grouped-header table components must have their own crops when present; full-page evidence is not enough.
- Capture additional screenshots after representative filter changes, tab switches, drawer/modal opening, drilldown, fullscreen, export preview, and relevant empty/error/auth states.
- Run deterministic baseline image diff when approved baselines exist. Record baseline path, current screenshot path, diff path, thresholds, masked regions, and `VDIFF-*` findings.
- If no baseline exists, save baseline candidates and record deterministic regression as `baseline missing`, not pass.
- Send screenshots to a multimodal model using `$quality-gate-validation` when the model/service is available and explanatory visual review is in scope.
- Record `VIS-*` findings for layout offset, excessive blank area, text overlap, graphic overlap, chart/table/card too small, clipping, unreadable labels, nonblank rendering failures, broken proportions, stale prototype residue, and broken scroll behavior.
- Record `VIS-*` findings for SVG/canvas/ECharts/custom-graphic distortion: warped curves, stretched maps, elliptical circles/radars, squeezed gauges, non-uniformly scaled icons, or paths whose curvature changes when the container resizes.
- Record `VIS-*` findings for duplicate component titles, cramped/narrow/tiny components, unbalanced peer-component strips, and text-graphic collisions where business-question text, labels, legends, chart marks, cards, or diagram nodes overlap or visually merge.
- Record `VIS-DUPLICATE-TITLE` when a block-owned title and an internal chart/table/KPI body title or metric label render the same normalized text in the same card. For KPI widgets, body metric labels that duplicate `widget.title`/`displayTitle` should be hidden by default unless `showBodyMetricLabel` is explicitly justified by multi-metric disambiguation.
- Record `VIS-*` or `RPT-*` findings for DOM structural failures: wrong card count, hidden extra cards without overflow affordance, unapproved bounding-box overlaps, duplicate category labels, or chart categories whose rendered order/count differs from the source/config contract.
- Record `VIS-KPI-VALUE-OFFCENTER` when a KPI/metric card allocates a value row but the measured `value + unit` group is vertically off center, uses value-row baseline alignment as the main layout strategy, or leaves the main numeral visually undersized while auxiliary text consumes the top.
- Record `VIS-*` findings for ECharts text-node collisions, including dual-axis Combo top legends overlapping or crowding left/right y-axis names such as units, because these collisions often occur inside one SVG/canvas chart even when outer DOM containers do not overlap.
- Record `VIS-*` findings from component crops when verifiable rules fail: Combo chart shared x-axis, axis units, dual-axis semantics, series limit, plot height, target/reference label, or tooltip evidence fails; Composite Panel lacks one shared topic, one primary child, child roles/priorities, primary visual weight, content-height budget, shared filter/legend/unit context, linked interaction, responsive fallback, or parent/child state scope; Analysis & Insight lacks `analysisInsightContract`/subtype, conclusion-before-evidence, evidence/affected object/comparison or insufficient-data state, action/detail/trust/source/freshness, tooltip/detail path, component-local filter scope, or stable loading/empty/error/no-permission/data-delay state; trend chart bottom legend crowds x-axis labels, donut/pie labels/guide lines/legend/title/center text collide, squeeze each other, clip, or leave card bounds, radar dimension labels/legend/filter/metric strip collide, radar is elliptical or stretched, scatter/bubble axes lack units, point labels crowd the point cloud, bubbles obscure too many points, quadrant/reference labels collide, parallel-coordinate axes lack object/dimension/axis evidence, axisGap or plot height is too small, ordinary lines form unreadable clutter, Top/anomaly/selected lines are indistinguishable, brush/filter/legend zones collide, tooltip cannot recover object values, map shapes/routes/coordinates are stretched, map visualMap/legend covers key geography, map labels crowd the basemap, dense points lack clustering/heatmap, dense flows lack TopN, candlestick candles are unreadable or too thin without dataZoom, volume bars are not aligned to candle centers, MA/indicator lines overpower candles, market red/green convention is wrong or undocumented, crosshair/tooltip cannot recover OHLC values, boxplot boxes/median/whiskers are unreadable, outliers overplot, category labels collide, grouped boxes are too dense, whisker/outlier rule is missing, five-number tooltip is unavailable, heatmap visualMap is missing or unclear, heatmap cells are too tiny, row/column labels collide, all-cell values crowd the matrix, missing values look like zero, correlation heatmap lacks diverging scale, sunburst lacks hierarchy/angle metric evidence, uses negative/rate/score as angle, lacks visible depth/ring budget, lacks TopN/其他, forces labels into tiny sectors, center text is clipped, color legend/visualMap is unclear, breadcrumb/drilldown is missing for deep levels, or tooltip cannot recover path/value/total-share/parent-share, treemap lacks hierarchy/area metric evidence, uses negative/rate/score as area, lacks TopN/其他, forces labels into tiny rectangles, parent groups are unreadable, color legend/visualMap is unclear, breadcrumb/drilldown is missing for deep levels, or tooltip cannot recover path/value/share, path chart lacks start/end/order, main path is not dominant, branches are all expanded into clutter, arrows point into node interiors, path labels collide, drop-off/abnormal paths overpower the main path, legends/search/local filters overlap the path, node/link tooltip is unavailable, Sankey lacks source-target-value evidence, layers/stages are unclear, flow body is too small, node/link density lacks TopN/其他, all ribbons are saturated, labels collide, loss/unknown flow disappears, legend/local filters overlap the flow, node/link tooltip is unavailable, tree chart lacks root/parent-child evidence, expands all nodes into clutter, visible depth is not controlled, node labels overflow boxes, connectors cross labels or do not stop at node edges, `+N`/aggregation is missing for dense children, legends/search/local filters overlap the tree, node tooltip is unavailable, relation graph nodes/edges collapse into a hairball, ordinary labels or edge labels overcrowd the network, source/target direction is unreadable, legends/search/zoom controls overlap the graph, fitView clips nodes or edges, node/edge tooltip is unavailable, complex/grouped table headers lack columnTree/leaf fields/computed spans, group-to-leaf alignment, fixed whole-header behavior, frozen row/primary columns, filter separation, tooltip definitions, density fallback, or useful body rows, Pivot Tables lack row/column/measure/aggregation evidence, fixed header/frozen row dimension, readable totals, density fallback, exact cell tooltip, or a real S2/project analytical renderer, KPI core value is not centered or occupies less than 40% of body height, or tables with more than 8 columns/natural groups use a flat header without reason.
- Record `VIS-*` findings for title-node collisions: section headers, stage/layer/lane titles, group captions, or column labels sitting on top of cards, node cards, connector paths, badges, legends, or child labels. The page cannot pass visual QA when this affects a key diagram or first-viewport component.
- Re-capture, re-diff, and re-review affected screenshots after repairing `blocker` or `major` `VDIFF-*` or `VIS-*` findings.

## Route And Surface Checks

- Direct entry, refresh, navigation, route params, tabs, drawers, modals, back/close, and fullscreen behavior.
- For embedded or micro-frontend pages, host mount point and base path behavior.

## Layout Checks

- Run the anti-AI design gate on the rendered page: primary surfaces must not rely on generic "modern SaaS/AI" aesthetics, empty slogans, interchangeable assets, first-screen-only polish, decorative motion, missing state coverage, missing accessibility, or sample-like hardcoded implementation. Record `AI-*` findings with evidence and severity.
- Run the report decision anti-AI gate on report surfaces: verify the page is not only a dashboard shell. Record `RPT-*` findings for generic KPI metrics without formulas, default `4 KPI + charts + list` layout, decorative chart variety, missing diagnostic story, all-clean mock/provider data, selected-state-only filters, visual polish that harms numeric reading, missing industry vocabulary, missing next action, or report-designer shells without data binding and validation behavior.
- For every primary report metric visible at runtime, verify available evidence for formula/denominator, grain, period/snapshot, source/freshness, unit/precision, baseline, owner/action, and drilldown/detail/export behavior. Missing evidence is at least a report decision QA gap.
- Verify the runtime data story path where the report claims to support decision-making: current state -> target/baseline gap -> time movement -> driver/dimension contribution -> abnormality/trust signal -> detail records -> owner/action.
- Verify Analysis & Insight components in the story path: conclusion cards, insight cards, anomaly/risk notes, attribution/recommendation cards, data-quality/definition notes, forecast notes, annotations, and explanatory states expose evidence/action/trust/source/freshness rather than generic prose.
- Verify mock/offline/provider sample data is not only smooth, balanced, and positive. At least one non-default filter branch, long label or high-cardinality case, missing/zero/extreme value, freshness/source marker, permission-limited state, or reconciliation case should be visible or named when relevant.
- Verify report filters, KPI/chart clicks, table row actions, drawers, exports, refresh, and share links carry active period, permission, filter, and snapshot/batch context forward. Context loss is an `RPT-STATIC-FILTERS` or linkage defect.
- Charts, tables, legends, labels, KPI cards, buttons, drawers, modals, sticky areas, table scrollbars, and toolbars fit their containers.
- Page shell titles and block-owned titles are not duplicated inside component bodies through chart/table/KPI internal title options.
- KPI widgets separate `displayTitle` and `metricName`: `displayTitle`/`widget.title` is the visible card title, while `metricName` stays in tooltip/export/口径/detail metadata. If a block title exists and the body metric label is the same or near-same text, `showBodyMetricLabel` must be false or absent.
- Block-owned title/function bands preserve the default left-title/right-function-area contract when present. Right function area uses capsule for one component-local filter with `2-4` short values and fit proof, dropdown for one local filter with `>4` values, long labels, or failed width fit, filter panel for multiple local filter groups, and lightweight links for detail actions; controls stay on one line and do not crowd the title.
- Component-internal local filters are verified as current-component scoped: changing them updates only the affected component/local group over already loaded bounded data, does not change page/global API/query scope unless separately modeled, and does not alter permission scope, pagination, export scope, or other widgets.
- Component-internal local filters must not overlay or compress KPI values, chart plots, legends, axes, table headers, pagination, or empty/loading/error/no-permission messages. If the title band cannot fit, the filter should collapse to dropdown or panel before harming the component body.
- Peer component groups use balanced `M * N` layouts where possible, such as 4 -> `2 * 2`, 6 -> `3 * 2`, 8 -> `4 * 2`, and 9 -> `3 * 3`; components are not narrow, tiny, crowded, or unreadable.
- Business-question text, conclusion text, labels, legends, chart marks, table cells, cards, diagrams, and controls do not overlap, stack, or visually merge.
- Section headers, group captions, layer/stage/lane labels, and column titles reserve independent title bands and maintain at least 16px safe spacing from the nearest card, node, connector, chart mark, legend, or child label.
- Verify dense data, long text, empty states, and loading states do not resize fixed-format UI unexpectedly.
- For composite parent blocks with internal sub-blocks, verify no-data masks use the correct hierarchy: all child sub-blocks empty shows one whole parent-block mask; partial child emptiness masks only affected sub-blocks, including each affected sub-block title/control area plus component body.
- Verify hover and `focus-visible` states for interactive cards, KPI tiles, chart/table containers, navigation items, toolbar controls, and local filter chips. They should preserve geometry and use in-bounds border/outline/glow; clipped borders, cut shadows, or offset/scale animation at grid edges are `VIS-*` defects.
- Check target desktop and mobile/tablet viewports when the page is responsive or embedded in variable containers.
- For perspective navigation density, verify each nav card carries at most two layers of primary information. If domain name, metric name, value, and focus point all appear together, record a layout defect unless the design uses a two-line structure, intentional horizontal navigation, dropdown perspective selector, tooltip, or overview detail area.
- For fixed-height cards and navigation items, verify implementation evidence for height budget: padding, explicit line-height values, row count, gaps, badge/status/footer heights, and `requiredContentHeight <= componentHeight`. Missing budget evidence is a QA gap; failing budget or DOM overflow is a `VIS-*` defect.
- For ECharts, AntV S2, canvas, and virtualized tables, verify nonblank rendering, resize behavior after container changes, and cleanup/dispose behavior after route changes or tab switches.
- For components mapped or labeled as ECharts standard charts, verify chart-engine fidelity: source/runtime uses an ECharts instance or approved wrapper, visible marks come from data-driven ECharts `option`/`series`, and tooltip/legend/emphasis behavior is available when relevant. Import-only ECharts paired with hand-authored SVG/HTML/CSS/canvas bars, lines, pies, gauges, maps, parallel-coordinate axes/lines, Sankey ribbons, sunburst arcs, tree nodes/connectors, relation graph nodes/edges, axes, or legends is a `VIS-*`/implementation QA failure.
- For ECharts stacked bar/column/progress charts, inspect option/config and crop: touching series share the same `stack` and category index, source rows are aligned, internal stacked joins have square corners, only outer visible stack ends are rounded, segment `borderWidth` is `0` unless an intentional divider is documented, and no background sliver, overlap notch, or rounded internal seam appears between segments at normal or narrow width.
- For Gauge charts, inspect option/config and crop: one bounded progress/status metric exists, min/max range, current value, unit, `series.type: 'gauge'`, start/end angle, radius, center, arc width, `gaugeAreaH` or equivalent body budget, target/threshold/status semantics, business color direction, clamp/overflow behavior with true displayed value, local-filter fit, sparse ticks, and tooltip exact payload are declared; center value, unit, arc, ticks, target marker, status text, local filters, and state messages stay in reserved zones.
- For Combo charts, inspect option/config and crop: shared ordered x-axis exists, bar metric/unit and line or target metric/unit are declared, left/right y-axis mapping is visible, dual-axis rationale and units are clear when used, visible series count is <=4, category labels are sampled/scrolled when dense, legend and local filters are separate, target/reference labels do not collide, plotH remains >= CH * 0.48, and axis-trigger tooltip exposes exact bar, line, target, unit, gap/attainment, period, and source values.
- For dual-axis ECharts Combo charts with a top legend, inspect `legend.top/left/right`, `grid.top`, `grid.containLabel`, both y-axis `name`, `nameLocation`, `nameGap`, `nameRotate`, `nameTextStyle`, and axis-label unit formatting. Browser QA must prove that the legend item text/markers and left/right y-axis name text keep at least `8px` visual gap and have no overlap greater than `4px²` at target viewports. `grid.containLabel: true` without this axis-name/legend collision check is not a pass.
- For trend/cartesian ECharts charts with visible x-axis labels and bottom legends, inspect option/config and crop: `grid.containLabel` is `true`, `grid.bottom >= 56px`, and the legend has clear safe distance from axis labels.
- For scatter/bubble charts, inspect option/config and crop: X/Y metric names and units are visible or in subtitle, axis ranges/baselines are declared, point count drives radius/opacity/label density, bubble size uses bounded sqrt mapping when present, reference lines/quadrants/trend lines are weak and data-driven, permanent labels are key-only, and tooltip exposes exact object values.
- For parallel-coordinate charts, inspect option/config and crop: object/sample identity fields exist, `parallelAxis` or dimension definitions declare `3-12` ordered metrics, each axis has unit/range/direction or declared standardization, `axisGap >= 56px`, `plotH >= CH * 0.48`, ordinary-line opacity follows sample count, sampling/aggregation is declared for dense samples, Top/anomaly/selected lines are distinguishable, brush/filter/legend zones stay separate, and tooltip exposes exact object values across visible dimensions.
- For map/geographic charts, inspect option/config and crop: geography field or lon/lat binding exists, map resource/projection is declared, fit/zoom/center preserve aspect ratio, `visualMap`/legend matches the data layer, basemap is weak, labels are key-only, dense points use clustering/heatmap, dense flows use TopN, missing coordinates/region mismatches have states, and tooltip exposes exact region/point/flow values.
- For candlestick/K-line charts, inspect option/config and crop: sorted OHLC fields exist, OHLC relationships are valid, price unit and market color convention are declared, `mainChartH` keeps the K-line body dominant, candle body width follows visible count, volume bars align with candle centers, MA lines are limited, technical indicators stay secondary/off by default, time labels are sampled, dataZoom/recent-window exists for dense histories, and tooltip exposes exact OHLC/volume/MA values.
- For boxplot charts, inspect option/config and crop: raw-sample or precomputed-statistics contract exists, sample counts and thresholds are declared, Q1/median/Q3/IQR and whisker/outlier rule are explicit, plot height keeps boxes/whiskers readable, category/group density is bounded, outliers use stable jitter or aggregation, permanent labels are key-only, and tooltip exposes five-number summary plus outlier details.
- For heatmap charts, inspect option/config and crop: row dimension, column dimension, value metric, aggregation grain, unit, visualMap/color-scale type, color range, missing-vs-zero encoding, row/column density, cell size, label sampling, value-label threshold, highlight/anomaly rule, and tooltip exact-value payload are declared; matrix cells, labels, visualMap, and local filters stay in reserved zones.
- For funnel charts, inspect option/config and crop: ordered stage dataset/schema, stage order, metric basis/unit, shared population or cohort rule, entry value, final value, entry share, stage conversion, drop value/rate, total conversion, stage-count density/fallback, local filter scope, target/comparison encoding when present, and tooltip exact-value payload are declared; stage labels, bars/funnel body, value/share labels, conversion/loss markers, target markers, legends, local filters, metric strip, and state messages stay in reserved zones.
- For treemap/rectangular tree maps, inspect option/config and crop: hierarchy fields or `children` exist, parent/leaf aggregation and non-negative additive area metric are declared, optional color metric/visualMap semantics are clear, Top N/`其他` or drilldown handles dense leaves, visible depth and breadcrumb/drilldown are declared when deep, label thresholds prevent tiny-rectangle text, parent groups remain readable, and tooltip exposes full path, value, percent of total, percent of parent, color metric, source, and aggregation rule.
- For sunburst charts, inspect option/config and crop: hierarchy fields or `children` exist, parent/child aggregation and non-negative additive angle metric are declared, optional color metric/visualMap semantics are clear, visible depth/ring-width budget keeps rings readable, Top N/`其他` or drilldown handles dense siblings, sector label thresholds prevent tiny-arc text, center content fits, breadcrumb/drilldown is declared when deep, and tooltip exposes full path, value, percent of total, percent of parent, color metric, source, and aggregation rule.
- For path/user/process path charts, inspect option/config and crop: step/node dataset/schema, directed transition dataset/schema, start node, end node, order/layer, metric basis, conversion/drop-off formulas, path depth, Top N and "other" aggregation, node/link density, main-path/branch strategy, path-width mapping, key-only labels, legend/filter separation, expand/collapse or fallback behavior, and node/link tooltip exact payload are declared; nodes, links, arrows, labels, legends, filters, and state messages stay in reserved zones.
- For Sankey diagrams, inspect option/config and crop: node dataset/schema, directed link dataset/schema with `source`/`target`/`value`, layer/stage order, metric unit, non-negative value handling, Top N/`其他` aggregation, node/link density tiers, flow-width/color mapping, key-only labels, legend/filter separation, hover/click adjacency highlight, loss/unknown/other node handling, and node/link tooltip exact payload are declared; nodes, ribbons, labels, legends, local filters, metric strip, and state messages stay in reserved zones.
- For tree/hierarchical tree charts, inspect option/config and crop: root node, node dataset/schema, parent-child schema or children array, depth/layer, visible depth/default expanded levels, node count, max child count, Top N/`+N` aggregation, orientation, expand/collapse behavior, search/locate when dense, label strategy, legend/filter separation, circular-reference/multiple-parent handling, and node tooltip exact payload are declared; root, visible levels, child branches, connectors, labels, expand controls, legends, filters, search, and state messages stay in reserved zones.
- For relation/network graph charts, inspect option/config and crop: node dataset/schema, edge dataset/schema, node ids, edge source/target resolution, relationship direction/weight, layout type, node/edge density strategy, node size and edge width mapping, label limits, legend/filter separation, roam/zoom/drag/reset/search behavior, fitView, missing node/edge states, and node/edge tooltip exact payload are declared; nodes, edges, arrows, labels, legends, filters, search, and zoom controls stay in reserved zones.
- For donut/pie charts in small cards or compact sub-blocks, inspect option/config and crop: donut is the report default; category count is `2-6` preferred and `<=8` before merge; `Top5 + 其他` or an approved merge rule is applied when categories exceed budget; negative values are rejected; all-zero values show empty/all-zero state; bottom legend is the default for narrow cards; right legend has a passing width budget and is used only when outside labels are disabled or key-label-only; `legendBandHeight`, `labelLineBudget`, `radius`, `innerRadius` when donut, and `center` are declared; labels have max width/wrapping or truncation disclosure; low-share categories may hide permanent outside labels while tooltip/legend exposes full values; `hideOverlap` is enabled, `bleedMargin` and `edgeDistance` are configured where supported, and no title/legend/label-line/label/center collision or out-of-bounds rendering exists.
- For radar charts, inspect option/config and crop: dimension count is `5-8` preferred and `<=10` maximum, visible series count is `<=3`, mixed raw units are standardized to a declared score scale, `radar.center` and `radar.radius` come from a measured plot viewport, the coordinate system remains circular, dimension labels sit outside the ring without collision, value labels are key-only, local filters are separate from legend, and tooltip includes exact/raw values.
- For KPI card crops, verify the core value zone is visually centered and occupies at least 40% of the card body height; title/subtitle/helper copy must not crowd the top while leaving a large unused blank area.
- For KPI title ownership, inspect config/source and DOM: block/card header title, body metric label, `displayTitle`, `metricName`, and `showBodyMetricLabel`. A single-metric KPI card must not visibly render both the block title and the same body metric label; keep the hidden metric identity available through tooltip/export/口径 metadata.
- For KPI/metric card DOM checks, inspect selectors such as `.metric-value-line`, `.metric-value`, `.metric-unit`, `.metric-comparisons`, and `.metric-footer` or their project equivalents. The measured `value + unit` group must be centered in the value anchor viewport within `8px`, the primary numeral glyph should normally occupy `22-28%` of the value anchor viewport for standard/enhanced primary cards, and `align-items: baseline` must not be the value row's main vertical placement strategy. For `W >= 360px` and `H >= 180px`, values fixed at small sizes such as `36px/40px` need fit rationale; `40-44px` is allowed when width/height checks pass.
- For Analysis & Insight component crops, inspect config/source and crop: `analysisInsightContract` or equivalent subtype/family metadata, one main conclusion before evidence, evidence/affected object/comparison/change/reason/source/freshness or explicit insufficient-data state, recommended action/detail route or trust/definition disclosure, component-local filter scope when present, tooltip/detail payload, and loading/empty/error/no-permission/data-delay states. Copy such as `智能洞察`, `建议关注`, or `有所变化` without concrete evidence/action/trust is a QA finding.
- For table/analytical-grid crops, verify more than 8 visible columns or natural field groups use complex/grouped headers; otherwise record the flat-header exception.
- For Composite Panel crops, inspect config/source and crop: `compositePanelContract`, one shared topic, analysis sequence, primary child id, child roles/priorities/min sizes, default `2-3` children and normal max `4`, primary visual weight `50-70%`, `contentH >= CH * 0.60`, panel-level local filters, child-only filter exceptions, shared legend/unit behavior, linked hover/click state, short detail preview, responsive fallback, parent/child loading/empty/error/no-permission states, and absence of equal-weight mini-dashboard or nested-card clutter.
- For complex/grouped table-header crops, inspect config/source and crop: `columnTree` or nested grouped columns, business group names, real leaf fields, unit/definition metadata, computed `colSpan`/`rowSpan` or max-depth/leaf-count rules, parent group width aligned to visible child columns, fixed whole-header behavior, frozen row/primary columns during horizontal scroll, component-local filter vs per-column header-filter separation, restrained sort/filter/definition icons, tooltip disclosure, group collapse/column settings/fullscreen fallback, loading/empty/error/no-permission states, and at least `4` useful body rows unless it is a named preview.
- For Detail Table crops, inspect config/source and crop: row grain, primary key/object field, default sort, prioritized visible columns, type-specific width/alignment, numeric right alignment, lightweight status tags, capped operation column, fixed header/frozen columns when needed, table-body height and visible-row budget, pagination/search/sort/export scope, row detail/action payload, tooltip disclosure for truncated cells, and loading/empty/error/no-permission states.
- For Pivot Table crops, inspect config/source and crop: visualType `pivot`, S2/project analytical renderer, row dimensions, column dimensions, measure metadata, aggregation formulas/functions, percentage/rate numerator-denominator rules, subtotal/grand-total placement, natural time sorting, frozen row dimension and fixed column header behavior, `pivotAreaH` and visible-row budget, row/column density fallback, conditional-formatting limits, tooltip/drilldown payload, and loading/empty/error/no-permission states.
- For SVG, canvas, ECharts custom graphics, maps, gauges, radars, graphs, Sankey, and complex diagrams, verify aspect-ratio preservation at target viewports. Shape-sensitive graphics should scale uniformly or relayout responsively, not stretch to fill both axes.
- For Element Plus controls, verify CSS/theme is loaded, popper overlays attach above the report shell, select/date/cascader dropdowns are not clipped by card overflow, dialogs/drawers preserve filter context, focus states are visible, and disabled/loading/error states render correctly.
- For Chinese report metrics, verify rate/change/completion labels show `%` rather than `pt`, `p.p.`, or `percentage point` unless explicitly required.
- Verify change-rate and variance-rate indicators use positive-red-up and negative-green-down SVG/icon+text semantics, with neutral styling for zero.
- For HTML-replica or custom layouts, verify the page uses global UI tokens for palette, typography, spacing, radius, shadows, semantic states, and control styling instead of copied one-off inline colors.
- Check keyboard focus, visible focus states, close/back escape behavior, and basic screen-reader labels for custom controls when those controls are part of the delivered workflow.
- Search primary headings, CTAs, summaries, empty/error states, and helper text for generic claims such as "赋能", "智能化", "一站式", "重新定义", "无缝", "提升效率", "Transform your workflow", "Unlock the power", or "Supercharge"; record a finding unless the same text also includes concrete user action, data object, condition, system behavior, or evidence.

## Evidence Quality

- QA notes must list the screenshot path for every full-page and component crop used in judgment.
- Findings must reference verifiable criteria, such as exact option fields, pixel/spacing requirement, collision/clipping evidence, column count, or crop path.
- Do not use only subjective wording such as "视觉舒适", "比较协调", or "看着正常" as a pass/fail basis.
