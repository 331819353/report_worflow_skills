interface:
  display_name: "报表原型全流程工作流"
  short_description: "仅在请求含“原型”时串联报表原型需求、映射、数据、筛选、交互、视觉、实现、部署与URL返回"
  default_prompt: "Use $report-design-workflow only when the user request contains 原型, then create, implement, validate linkage, deploy, and return a URL for an end-to-end business report prototype."
