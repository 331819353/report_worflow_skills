interface:
  display_name: "Frontend Development Workflow"
  short_description: "Connect prototype mocks to APIs and QA the UI"
  default_prompt: "Use $frontend-development-workflow to replace prototype mock data with HTTP APIs, check functional and copy issues, and verify the frontend runs successfully."
