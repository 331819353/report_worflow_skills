interface:
  display_name: "前端环境与部署验证"
  short_description: "验证 env、proxy、base path、路由、静态资源、build、preview、部署目标和最终 URL"
  default_prompt: "Use $frontend-env-deployment-verification to configure and verify frontend API base URLs, proxies, build/preview behavior, deployment paths, and final URLs."
