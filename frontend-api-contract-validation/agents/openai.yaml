interface:
  display_name: "前端数据契约校验"
  short_description: "校验 API、SDK、静态文件或实时数据源是否满足前端 UI 契约"
  default_prompt: "Use $frontend-api-contract-validation to compare frontend UI contracts with API, SDK, static-file, realtime, or data-source provider contracts before replacing mock data."
