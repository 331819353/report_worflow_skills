# Adapter Output Template

```markdown
## Frontend Adapter Design

- Consumer components:
- Provider type:
- Provider payload/source:
- Stable UI view model:
- Adapter location:
- Frontend compute policy:
- Filter/sort/page execution stage:
- Global filter input mapping:
- Component-internal local filter scope:
- Field mapping:
- Enum/unit/date rules:
- Pagination/list rules:
- Empty/error/auth rules:
- Missing information:
- Verification samples:
```

Keep the note short enough to guide implementation, but explicit enough that duplicate mapping does not reappear in components.
