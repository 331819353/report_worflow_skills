interface:
  display_name: "前端响应适配设计"
  short_description: "设计任意数据源到前端 view model 的适配层，保护组件 props 稳定"
  default_prompt: "Use $frontend-response-adapter-design to map API, SDK, file, realtime, or data-source payloads into stable frontend view models while replacing mock data."
