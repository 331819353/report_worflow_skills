interface:
  display_name: "前后端数据一致性测试"
  short_description: "逐组件核对后端接口返回与前端展示值，覆盖字段映射、单位精度、汇总排序、图表表格绑定和 mock 残留"
  default_prompt: "Use $frontend-backend-data-consistency-test to compare frontend report displays with backend API responses field by field."
