interface:
  display_name: "报表原型 Workflow"
  short_description: "编排报表/仪表盘/大屏原型，从需求、报表类型、组件映射、布局、模板、样式到运行验证和URL交付。"
  default_prompt: "Use $report-design-workflow to orchestrate report prototype work from requirements through report type, component mapping, layout, template use, implementation, QA, and URL handoff."
