interface:
  display_name: "报表组件风格设计"
  short_description: "规范报表组件视觉与响应式行为，覆盖高度预算、显式行高、Analysis & Insight、Composite Panel、多组件组合、ECharts图例/轴安全距、明细表格行列密度、透视表/S2交叉汇总、环图空间预算、KPI值区、复杂表头和防裁切。"
  default_prompt: "Use $report-component-style-design to design consistent responsive report components, including fixed-height card/KPI/navigation height budgets, explicit line-height, DOM overflow prevention, Analysis & Insight analysisInsightContract/subtype/conclusion/evidence/action/trust/source/freshness/state rules, Composite Panel compositePanelContract/primary-child/child-priority/shared-filter/contentH/internal-layout rules, ECharts legend/axis spacing, Detail Table row grain, primary key, visible columns, column width/alignment, default sort, tableBodyAreaH budget, row count, fixed header/frozen columns, pagination/search/export scope, row detail/action, exact tooltip, complex/grouped table headers with columnTree, business column groups, leaf field metadata, colSpan/rowSpan calculation, maxDepth <=3 by default, parent-width-from-leaf-width rules, fixed multi-level headers, frozen row/primary columns, component-local filter vs per-column header-filter separation, sort/filter icon limits, grouped-header tooltip definitions, Pivot Table/S2 row dimensions, column dimensions, measures, aggregation formulas, subtotal/grand-total rules, pivotAreaH budget, frozen row/column headers, row/column density, conditional formatting limits, drilldown tooltip, Combo shared-axis bar-line/target contract, axis units, series limits, plotH budget, exact tooltip, Gauge one-bounded-metric contract, min/max range, center value, gaugeAreaH budget, target/threshold/status semantics, exact tooltip, funnel stage schema, shared population/cohort, entry share, stage conversion, drop value/rate, total conversion, funnelArea budget, stage density, exact stage tooltip, path/user/process path step-link schema, start-end-order, pathArea budget, TopN branches, conversion/drop-off labels, legend/filter separation, Sankey source-target-value node/link schema, layer/stage order, sankeyArea budget, TopN+其他 aggregation, flow-width mapping, main-flow emphasis, loss/unknown handling, node-link tooltip, treemap/rectangular tree map hierarchy and non-negative additive area metric, TopN+其他 aggregation, label thresholds, color semantics, breadcrumb/drilldown, exact path/value/share tooltip, sunburst hierarchy path, non-negative additive angle metric, visible depth/ring-width budget, TopN+其他 aggregation, sector label thresholds, center content, breadcrumb/drilldown, exact path/total-share/parent-share tooltip, parallel coordinates object/dimension schema, per-axis range/unit/direction, standardization mode, axis-gap budget, line-opacity density, brush, highlight semantics, exact object tooltip, tree/hierarchical tree root-node and parent-child schema, visible depth, expand-collapse, TopN/+N aggregation, orientation, treeArea budget, node labels and tooltip, relation/network graph node-edge schema, layout type, node/edge density, label limits, legend/filter separation, fitView/zoom/drag, node-edge tooltip, matrix/time/calendar/correlation heatmap row/column/value contract, visualMap/color scale, missing-vs-zero encoding, cell-size and label-density rules, boxplot sample count, Q1/median/Q3/IQR, whisker/outlier rule, outlier density, five-number tooltip, candlestick/K-line OHLC fields, market color convention, candle density, volume/indicator height budget, crosshair/dataZoom, map/geographic chart region-code or lon/lat binding, map resource/projection, aspect-safe fitBounds, visualMap/legend, map label density, clustering/heatmap/TopN fallback, scatter/bubble X/Y units, point density, bubble size mapping, quadrant/reference-line behavior, donut/pie category count, TopN+其他 merge rule, negative/all-zero handling, legend placement, legendBandHeight, labelLineBudget, radius, innerRadius, center, low-value label handling, KPI value-zone hierarchy, AntV S2 tables, capsule switches, and dropdown controls."
