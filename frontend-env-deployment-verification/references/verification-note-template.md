# Environment Verification Note Template

```markdown
## Frontend Environment And Deployment Note

- Project/runtime:
- Package manager and commands:
- Env files/variables:
- Provider configuration:
- Proxy behavior:
- Router/base path:
- Static assets:
- Build output:
- Preview/deploy URL:
- Commands verified:
- Production readiness:
- Backend/API base URL:
- Provider/auth/env mode:
- Deployment target:
- Rollback or restore path:
- Observability/log access:
- Remaining blocker:
```

Use `Provider configuration` for API URLs, GraphQL endpoints, SDK init values, static data paths, or realtime hosts. Use `Remaining blocker` only for external missing credentials, target URLs, network access, or deployment commands.
