interface:
  display_name: "后端接口契约校验"
  short_description: "校验后端接口是否与前端 mock/展示契约一致，覆盖字段、空态、分页、筛选、错误、转换样例和性能边界"
  default_prompt: "Use $backend-api-contract-validation to compare backend API responses with frontend mock/display contracts and verify request, error, transformation, and performance behavior."
